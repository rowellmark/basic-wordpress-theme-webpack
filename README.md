## Basic Wordpress Theme with Webpack

### Setup(don't clone)
- Clone repository
- Run `npm install` to install the dependencies
- Run `npm run watch` to watch for file changes, or run `npm run build` to compile the changed files  
